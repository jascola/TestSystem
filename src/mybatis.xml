<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

	<!-- mappers指要加载的映射文件 -->
	<mappers>
		<mapper resource="resource/mapper/user-mapper.xml" />
		<mapper resource="resource/mapper/choice-mapper.xml" />
		<mapper resource="resource/mapper/option-mapper.xml" />
		<mapper resource="resource/mapper/recogniz-mapper.xml" />
		<mapper resource="resource/mapper/score-mapper.xml" />
		<mapper resource="resource/mapper/subject-mapper.xml" />
		<mapper resource="resource/mapper/completion-mapper.xml" />
	</mappers>
</configuration>